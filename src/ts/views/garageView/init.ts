export const garageWrapper = document.createElement('section');

/**
 * @description Initializes the garage view
 */
export function init() {
  garageWrapper.classList.add('garage');
  return garageWrapper;
}
